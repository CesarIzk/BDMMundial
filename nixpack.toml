[phases.setup]
nixPkgs = ["php82", "php82Extensions.pdo", "php82Extensions.pdo_mysql", "php82Extensions.mbstring"]
nixpkgsArchive = "aea547d4ec4ac01efe763cde7ccfbb263d5e2b86"

[phases.build]
cmds = ["composer install --no-dev --optimize-autoloader"]

[phases.start]
cmd = "php -d extension=pdo.so -d extension=pdo_mysql.so -S 0.0.0.0:$PORT -t public"